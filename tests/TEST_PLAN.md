# Manual test plan

- Verify `pip install -r requirements.txt` succeeds.
- Verify the example YAML loads without validation errors.
- Verify invalid poll files fail with clear messages.
- Verify `--dry-run` forces all jobs to dry-run mode.
- Verify jobs print in publish time order.
